<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Plant {{ token }}s with Bitcoin, Ethereum and more crypto currencies.">
    <meta name="author" content="@wehmoen">
    <link rel="icon" href="{{ logo }}">

    <title>Plant {{ token }}s with Bitcoin, Ethereum and more crypto currencies.</title>

    <link rel="canonical" href="https://getbootstrap.com/docs/4.0/examples/checkout/">

    <!-- Bootstrap core CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">

</head>

<body class="bg-light">

<div class="container">
    <div class="py-5 text-center">
        <img class="d-block mx-auto mb-4 rounded" src="{{ logo }}" alt="" width="72" height="72">
        <h2>Buy {{ token }}</h2>
        <p class="lead">
            Enter the amount of {{ token }}s you want to plant as well as your steem account that should appear in the donation list.
            <br>You can optionally also set a memo.
            <br>
        <h4>${{ price }} USD plants 1 {{ token }}</h4>
        </p>
    </div>

    <div class="row">
        <div class="col-12">
            <form action="/checkout" method="post" class="needs-validation">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label for="firstName">Quantity:</label>
                        <input type="number" step="{{ step }}" min="{{min_purchase}}" class="form-control" id="quantity" name="quantity" placeholder="" value="{{min_purchase}}"
                               required>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label for="username">Steem Account</label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                                <span class="input-group-text">@</span>
                            </div>
                            <input type="text" class="form-control" name="receiver" placeholder="Username" required>
                        </div>
                    </div>
                </div>
                <div class="mb-3">
                    <label for="email">Memo <span class="text-muted">(Optional)</span></label>
                    <input type="text" class="form-control" name="memo" placeholder="">
                </div>

                <hr class="mb-4">
                <button class="btn btn-primary btn-lg btn-block" type="submit">Continue to checkout ($<span id="total">{{ total }}</span>USD) </button>
            </form>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-12">
            <p class="alert alert-info">
                <i><b>Save the Planet</b></i> with TREE. For each TREE token purchased we donate ${{ price }}USD to the #teamtrees project run by the popular YouTuber MrBeast.
            </p>
        </div>
    </div>

    <div class="row">
        <div class="col text-center">
            <h4>
                <a href="/donations">Show Planted Trees</a>
            </h4>
        </div>
    </div>
    <footer class="my-5 pt-5 text-muted text-center text-small">
        <p class="mb-1">&copy; {{ ""|date("Y") }} {{contact.name}}</p>
        <ul class="list-inline">
            <li class="list-inline-item"><a href="//steemit.com/@{{ contact.steem }}">Blog</a></li>
            <li class="list-inline-item"><a href="mailto:{{ contact.email }}?subject={{ token }}%20Purchase">Contact</a></li>
        </ul>

        <p class="mt-2 text-muted"><i>
                This project is not affiliated with MrBeast or teamtrees.org. We just want to offer everyone to plant trees with crypto.
                For proof of donation follow our Steem Blog: <a href="//steemit.com/@{{ contact.steem }}">@{{ contact.steem }}</a>.
            </i></p>
    </footer>
</div>

<!-- Bootstrap core JavaScript
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
        integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
        crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/holder/2.9.6/holder.min.js"></script>
<script>
    const price_per_token = {{ price_plain }};
    function updateTotal() {
        $('#total').text((price_per_token * parseInt($('#quantity').val())).toFixed(2))
    }

    $(() => {
        $('#quantity').on("change", updateTotal);
        $('#quantity').on("keyup", updateTotal);
    })
</script>
</body>
</html>
